<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="24dp">

    <TextView android:id="@+id/cityText" android:text="Cidade" android:textSize="24sp" />
    <TextView android:id="@+id/tempText" android:text="--°C" android:textSize="48sp" />
    <TextView android:id="@+id/descText" android:text="Descrição" android:textSize="18sp" />

</LinearLayout>